apiVersion: apps/v1
kind: Deployment
metadata:
  name: notificationservice
  namespace: {{ .Values.namespace }}
spec:
  replicas: {{ .Values.notificationservice.replicas }}
  selector:
    matchLabels:
      app: notificationservice
  template:
    metadata:
      labels:
        app: notificationservice
    spec:
      imagePullSecrets:
      - name: ghcr-secret
      containers:
      - name: notificationservice
        image: {{ .Values.notificationservice.image }}
        imagePullPolicy: {{ .Values.images.pullPolicy }}
        ports:
        - containerPort: {{ .Values.notificationservice.port }}
        env:
        - name: ASPNETCORE_ENVIRONMENT
          value: "Production"
        - name: ConnectionStrings__notificationserviceDb
          value: "Host={{ .Values.postgresql.host }};Port={{ .Values.postgresql.port }};Database=notificationservice_db;Username={{ .Values.postgresql.username }};Ssl Mode=Disable;"
        - name: Keycloak__Authority
          value: "{{ .Values.keycloak.url }}/realms/{{ .Values.keycloak.realm }}"
        - name: Keycloak__AdminUrl
          value: "{{ .Values.keycloak.url }}"
        - name: Keycloak__Realm
          value: "{{ .Values.keycloak.realm }}"
        - name: Keycloak__ClientId
          value: "{{ .Values.keycloak.clientId }}"
        - name: Keycloak__ClientSecret
          valueFrom:
            secretKeyRef:
              name: kidsproject-secrets
              key: keycloak-client-secret
        - name: Keycloak__Audience
          value: "account"
        - name: Keycloak__RequireHttpsMetadata
          value: "false"
        - name: Redis__ConnectionString
          value: "{{ .Values.redis.host }}:{{ .Values.redis.port }}"
        resources:
          requests:
            memory: {{ .Values.notificationservice.resources.requests.memory }}
            cpu: {{ .Values.notificationservice.resources.requests.cpu }}
          limits:
            memory: {{ .Values.notificationservice.resources.limits.memory }}
            cpu: {{ .Values.notificationservice.resources.limits.cpu }}
        livenessProbe:
          httpGet:
            path: /health
            port: {{ .Values.notificationservice.port }}
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: {{ .Values.notificationservice.port }}
          initialDelaySeconds: 15
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: notificationservice
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: notificationservice
  ports:
  - port: 80
    targetPort: {{ .Values.notificationservice.port }}
  type: ClusterIP