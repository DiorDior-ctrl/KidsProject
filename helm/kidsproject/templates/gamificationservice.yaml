apiVersion: apps/v1
kind: Deployment
metadata:
  name: gamificationservice
  namespace: {{ .Values.namespace }}
spec:
  replicas: {{ .Values.gamificationservice.replicas }}
  selector:
    matchLabels:
      app: gamificationservice
  template:
    metadata:
      labels:
        app: gamificationservice
    spec:
      imagePullSecrets:
      - name: ghcr-secret
      containers:
      - name: gamificationservice
        image: {{ .Values.gamificationservice.image }}
        imagePullPolicy: {{ .Values.images.pullPolicy }}
        ports:
        - containerPort: {{ .Values.gamificationservice.port }}
        env:
        - name: ASPNETCORE_ENVIRONMENT
          value: "Production"
        - name: ConnectionStrings__gamificationserviceDb
          value: "Host={{ .Values.postgresql.host }};Port={{ .Values.postgresql.port }};Database=gamificationservice_db;Username={{ .Values.postgresql.username }};Ssl Mode=Disable;"
        - name: Keycloak__Authority
          value: "{{ .Values.keycloak.url }}/realms/{{ .Values.keycloak.realm }}"
        - name: Keycloak__AdminUrl
          value: "{{ .Values.keycloak.url }}"
        - name: Keycloak__Realm
          value: "{{ .Values.keycloak.realm }}"
        - name: Keycloak__ClientId
          value: "{{ .Values.keycloak.clientId }}"
        - name: Keycloak__ClientSecret
          valueFrom:
            secretKeyRef:
              name: kidsproject-secrets
              key: keycloak-client-secret
        - name: Keycloak__Audience
          value: "account"
        - name: Keycloak__RequireHttpsMetadata
          value: "false"
        - name: Redis__ConnectionString
          value: "{{ .Values.redis.host }}:{{ .Values.redis.port }}"
        resources:
          requests:
            memory: {{ .Values.gamificationservice.resources.requests.memory }}
            cpu: {{ .Values.gamificationservice.resources.requests.cpu }}
          limits:
            memory: {{ .Values.gamificationservice.resources.limits.memory }}
            cpu: {{ .Values.gamificationservice.resources.limits.cpu }}
        livenessProbe:
          httpGet:
            path: /health
            port: {{ .Values.gamificationservice.port }}
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: {{ .Values.gamificationservice.port }}
          initialDelaySeconds: 15
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: gamificationservice
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: gamificationservice
  ports:
  - port: 80
    targetPort: {{ .Values.gamificationservice.port }}
  type: ClusterIP